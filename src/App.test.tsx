import "@testing-library/jest-dom";
import { waitFor } from "@testing-library/react";

/*
 * サンプルテスト
 */
describe("サンプルテスト", () => {
  test("[正常系]サンプルテスト", async () => {
    // 実行
    // render(<App />);

    // 検証
    waitFor(() => {});
  });
});
