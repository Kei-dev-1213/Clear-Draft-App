// ヘッダーの高さ
const HEADER_HEIGHT = "125px";

// フッターの高さ
const FOOTER_HEIGHT = "10px";

// コンテンツの横幅
const CONTENT_WIDTH = "1000px";

// コンテンツの高さ
const CONTENT_HEIGHT = `calc(100vh - (${HEADER_HEIGHT} + ${FOOTER_HEIGHT}))`;

export const CONSTANT = {
  HEADER_HEIGHT,
  FOOTER_HEIGHT,
  CONTENT_WIDTH,
  CONTENT_HEIGHT,
};
